/* =========================================================
   ZGOB APPAREL — Supabase connection settings
   Fill these in after creating your Supabase project:
   Project Settings → API → Project URL / anon public key.
   The anon key is safe to expose in frontend code — it's
   designed for this, and every table is locked down with
   Row Level Security (see supabase/schema.sql).
   ========================================================= */
const ZGOB_SUPABASE_URL = 'https://swedzkrubhgvtzvmhyyn.supabase.co';
const ZGOB_SUPABASE_ANON_KEY = 'sb_publishable_czh_iPWp0gZbgMgsrJ0XCQ__vKoI79o';
